import SwiftUI

enum IconVariant {
    case tiny // ~16pt - inline with captions or metadata
    case small // ~24pt - default, body or buttons
    case medium // ~32pt - section icons, larger buttons
    case large // ~48pt - hero areas, avatars
    case jumbo // ~64pt - decorative, dashboards
}
