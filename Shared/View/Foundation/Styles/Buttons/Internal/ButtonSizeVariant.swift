enum ButtonSizeVariant {
    case small // compact- ~32pt height
    case medium // default- ~40pt height
    case large // big CTA- ~48pt height
    case block // full-width (maxWidth = .infinity)
}
